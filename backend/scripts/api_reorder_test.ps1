<#
  The reorder endpoint and position persistence were removed from the project.
  This script used to test /api/services/reorder. It has been left as a note
  so you don't run it by accident.

  If you need a test that exercises the current API behavior (create/update/delete
  services or fetch /api/services), create a new script that calls those endpoints.
#>
Write-Host "Note: reorder endpoint removed; this script is a no-op."